server:
  port: ${EMPLOYEE_APP_PORT}
spring:
  application:
    name: employee-service
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE}
  instance:
    prefer-ip-address: true