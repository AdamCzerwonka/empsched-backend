spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/empshed_scheduling
    username: empshed
    password: empshed
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
eureka:
  client:
    enabled: false
management:
  endpoints:
    web:
      exposure:
        include: ''
